# Lint/type tooling, PINNED EXACTLY.
# Unpinned, CI installs whatever released that morning and a formatter release
# turns the build red with no code change — and disagrees with every local run.
ruff==0.16.5
black==26.5.1
mypy==2.3.1

# Type stubs. Preferred over an `ignore_missing_imports` override: real stubs
# type-check the call, an override only silences it.
types-PyYAML==6.0.12.20260815
# Matched to the installed pandas 2.3.x, NOT the latest release. pandas-stubs
# tracks pandas versions, so the 3.0.x stubs describe an API that pandas 2.3
# does not have — mypy would then check against the wrong library.
pandas-stubs==2.3.3.260113
types-requests==2.33.0.20260712
