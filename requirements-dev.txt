# The lock, not the declaration: local development and CI must install the
# same resolution the images do, or "works on my machine" is a real defence.
-r requirements-lock.txt
-r requirements-lint.txt

pytest==9.1.1
pytest-cov==7.1.0

# starlette 1.6's TestClient requires httpx2, NOT httpx — importing it without
# this raises RuntimeError at import time with that exact instruction. Test
# dependency only; the service itself makes no outbound HTTP calls.
httpx2==2.12.0
